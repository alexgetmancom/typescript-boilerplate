services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      BIND_HOST: 0.0.0.0
      DATABASE_URL: /app/data/app.db
      PORT: 8080
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
